import { NextConfig } from "next";

const nextConfig: NextConfig = {
  // images: {
  //   unoptimized: true,
  // },
  // output: "standalone",
  reactCompiler: true,
  // rewrites: () => [
  //   {
  //     source: "/:locale/home",
  //     destination: "/:locale",
  //   },
  // ],
};

export default nextConfig;
